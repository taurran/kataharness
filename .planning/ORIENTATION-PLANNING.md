# PLANNING-WINDOW ORIENTATION — written 2026-08-16 (trust-model grill closed; burn freezing)

> **Locked agent-orientation format (UX-15): MISSION → GUARDRAILS → CONTEXT → REPORT CONTRACT →
> YOUR BRIEF, copy block LAST, content lines undecorated (UX-7).** This is the PLANNING window of
> a TWO-WINDOW split — an execution window runs the Trust Model burn CONCURRENTLY. Honor the
> ownership fence or the windows collide.

━━ KATAHARNESS · AGENT ORIENTATION · PLANNING WINDOW ━━━━━━━━━━━
run: grill-and-backlog only · concurrent with: the Trust Model burn (other window)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## MISSION

Grill the next backlog items to convergence while the burn executes elsewhere. Queue, in
priority order: (1) **Backlog Burn mode** — full grill of BBM-1..13 → converged ledger (its
trust-model cross-reference block is BINDING input); (2) **the agent cadre** — full grill
(AC-1..13 + the 14-role roster + the GROUNDING AGENT addition + Kata Superpowers extraction +
the AC-5 Kitchen sequencing call); (3) **the learning graph** (BL-N16 — including the
job-scoped-vs-durable learning-scope taxonomy the trust-model grill seeded, TM-C7 rider 1);
(4) **the Kitchen** (BL-N08 — the operator wants this one LIVE and deep; offer it, let them
pick the sitting). PLANNING ONLY: no build dispatches, no code edits — D169 binds and the
building happens in the other window.

## GUARDRAILS — the two-window fence (violating this collides the windows)

- **THIS window OWNS:** `.planning/BACKLOG.md` · `.planning/DECISIONS.md` ·
  `.planning/DEFERRED.md` · spec dirs OTHER than trust-model (backlog-burn-mode/, agent-cadre/,
  learning-graph/, a new kitchen/ dir) · `CONTEXT.md` glossary adds.
- **THIS window NEVER touches:** `tools/**`, `skills/**`, `protocol/**`, `modules/**`,
  `adapters/**` (code = the burn's), `.planning/specs/trust-model/**`, `.kata/**`, any
  `burn/*` or `task/*` branch. If a grill resolution NEEDS a code fact, READ it; never edit.
- Work on a dedicated branch: `git checkout -b grill/planning-window` from the tip noted below.
- `HANDOFF.md`/`STATE.md`: append/update ONLY at this window's close, on this window's branch;
  the operator merges windows in their chosen order.
- Grill discipline: kata-grill tiers per item (burn-mode + cadre warrant advanced) · ONE
  question per interaction (D153/U1) · plain language (memory: grill-in-plain-terms) ·
  checkpoint the ledger after EVERY resolved branch · fresh-context convergence gate(s) before
  any ledger is called converged · ELEVATE exactly once per grill close · **learn_feed emit
  stays BLOCKED by 🔴 BL-X12 until the burn fixes it — surface, never run.**
- PD-1/PD-2 always. Wave never sprint. Backlog Burn never bare "burn". The vault is KIBAN.
  Kata Superpowers (AC-7 RULED — never "mindbridge superpowers"/"KataHarness Superpowers").
- **TM-A3 truth status binds:** any backlog item you touch gets/keeps its truth-status mark;
  BUILT is legal only with cited Verified evidence.

## CONTEXT — read in this order

| what | where |
|---|---|
| The trust-model rulings that BIND your grills | `.planning/specs/trust-model/GRILL-LEDGER.md` (converged) + `DESIGN.md` §10 feeds-map |
| Burn-mode grill input | `.planning/specs/backlog-burn-mode/GRILL-LEDGER.md` (BBM-1..13 + the trust-model cross-ref block at the tail) |
| Cadre grill input | `.planning/specs/agent-cadre/GRILL-LEDGER.md` (AC rulings + roster + grounding-agent addition + RESEARCH-AGENTS.md) |
| Learning-graph input | `.planning/specs/learning-graph/RESEARCH-HERMES-PI.md` + BL-N16's backlog entry (with its trust-model seam note) |
| The backlog + truth statuses | `.planning/BACKLOG.md` (priority table logic per the 2026-08-16 session) |
| What the OTHER window is doing | `.planning/specs/trust-model/{PLAN.md, BURN-CHARTER.md}` — READ-ONLY awareness |

## OPERATOR GATES (surface, never close): UX round C1 + freeze sign-off (sequenced AFTER the
combined rev-4 convergence — do not spend operator time on it early) · probes 2/6 (host
stability) · BL-N25 kiro · old-PAT revocation if still open.

## REPORT CONTRACT — before this window closes

☐ every grill ledgered + checkpointed · ☐ convergence gates fresh-context, never self-graded ·
☐ ELEVATE once per close · ☐ emits surfaced-as-blocked (X12) · ☐ no file outside the fence
touched · ☐ handoff block appended at close · ☐ truth statuses maintained

━━━━━ end orientation ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## YOUR BRIEF — ✂ copy below · paste into the PLANNING window ✂

Load KataHarness context per CLAUDE.md (prime directives first, then AGENTS.md).
Read .planning/ORIENTATION-PLANNING.md in full and honor its two-window fence:
this session is PLANNING ONLY (grills + backlog; no builds, no code edits) and
runs concurrently with the Trust Model burn in another window. Branch
grill/planning-window. Verify ground truth per the HANDOFF turnover block.
Queue in order: full grill of Backlog Burn mode (BBM-1..13, advanced), then
the agent cadre (advanced; incl. the grounding agent and Kata Superpowers),
then the learning graph (BL-N16 + the learning-scope taxonomy), then offer
the operator the live Kitchen grill. Trust-model rulings bind as input; the
learn_feed emit stays blocked by BL-X12. One question at a time, plain
language, checkpoint every resolution. The vault is Kiban. Wave, never
sprint.

✂ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ copy ends ━━━━━━━━━━━━━━━━━━━━━━
