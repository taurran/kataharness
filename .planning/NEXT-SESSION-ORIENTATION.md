# NEXT-SESSION ORIENTATION — written 2026-08-16 turnover (master @ de8578c)

> **Locked agent-orientation format (UX-15): MISSION → GUARDRAILS → CONTEXT → REPORT CONTRACT →
> YOUR BRIEF, copy block LAST, content lines undecorated (UX-7).** Authoritative state:
> `.planning/HANDOFF.md` (the TURNOVER block). This file is the paste companion.

━━ KATAHARNESS · AGENT ORIENTATION ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
run: post-merge execution era · session: next · branch: master
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## MISSION

Carry the backlog forward BOTH ways: grill + design the remaining items, AND code + implement
them FULLY — no stubs, no deferred-in-silence, no loop bypass — in full faith of the project's
spirit. The execution core is the enforcement program (M33 → M34 → N01 → N19 → N20/agent-cadre);
the UX system waits only on the operator's freeze sign-off.

## GUARDRAILS

- PD-1/PD-2 bind (`protocol/prime-directives.md`, loaded first, always). Done requires proof.
- **BBM-12: every run uses the ENTIRE loop** — wave-per-loop, re-loop on failed final eval. The
  conductor-driven bypass is recorded DRIFT (both prior Backlog Burns). Never repeat it; if the
  machinery blocks you, that IS the work (BL-M33/M34), not a reason to hand-roll.
- D169: nothing builds from a draft — grill → convergence gate → freeze, every item.
- D172: enforcement seams are deterministic CODE (the minimal-Python preference is relaxed
  exactly there); write lessons + doctrine entries AS you design, not at wrap.
- Dispatched authoring (KH-T13) · hybrid gating (BBM-1) · validators execute-the-tooling (AC-10)
  · challenger on adversarial validation only, step-up tier, opposite family (AC-11/BL-N10).
- Plain English always · wave never sprint · "Backlog Burn" in docs (BBM-13) · the vault is
  KIBAN · no internal codenames user-facing (UX-24) · uv run for all tests · manual pinned
  worktrees outside the repo root.

## CONTEXT — read in this order

| what | where |
|---|---|
| Authoritative state | `.planning/HANDOFF.md` — the TURNOVER block (§0–§5) |
| Current block only | `.planning/STATE.md` CURRENT (never the whole 1500-line file) |
| The execution core | 🔴 BL-M33 · 🔴 BL-M34 · 🔴 BL-N01 · BL-N19 · BL-N20 in `BACKLOG.md` |
| The agent cadre | `specs/agent-cadre/` — GRILL-LEDGER (AC-1..13 + roster) + RESEARCH-AGENTS.md |
| Backlog Burn mode | `specs/backlog-burn-mode/GRILL-LEDGER.md` (BBM-1..13) |
| The UX system | `specs/ux-rework/` — DESIGN (freeze-candidate) + CONVERGENCE-R1 (operator list) |
| What just happened | `specs/backlog-burn-02/OBSERVATIONS.md` + CHANGELOG [Unreleased] |

## REPORT CONTRACT — before this next session ends

☐ any grill opened is ledgered · ☐ any build ran INSIDE the loop with its evidence emitted
durable · ☐ operator gates surfaced not closed (UX freeze · AC-7 naming · 🔴 PAT · T-03 ·
probes 2–6) · ☐ lessons/doctrine written in-flight · ☐ handoff refreshed at close

━━━━━ end orientation ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## YOUR BRIEF — ✂ copy below · paste into new session ✂

Load all KataHarness context per CLAUDE.md (prime directives first, then AGENTS.md).
Read .planning/HANDOFF.md's TURNOVER block in full and .planning/STATE.md's CURRENT
block only. Verify ground truth: git status --porcelain (empty) · branch master ·
cd tools && uv run python scripts/gauntlet.py (4/4, pytest 4518) · installed
kata-orchestrate is 0.18.0. The charter: grill + design the remaining backlog items
AND code + implement them FULLY, in full faith — BBM-12 (the entire loop, always),
D169 (freeze before build), D172 (deterministic seams), PD-1/PD-2. Execution core:
BL-M33 then M34 then N01 then N19 then N20/agent-cadre. Surface the operator gates
(UX freeze sign-off, AC-7 naming, PAT rotation, T-03, probes 2-6); never close them
yourself. The vault is Kiban. Wave, never sprint. Backlog Burn, never bare "burn".

✂ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ copy ends ━━━━━━━━━━━━━━━━━━━━━━
