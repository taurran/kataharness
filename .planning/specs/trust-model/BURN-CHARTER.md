---
spec: trust-model
artifact: "burn charter — operator-ruled 2026-08-16; binds the plan-author and the executing session"
status: ruled (freeze of the PLAN itself still pending per D169)
---

# Backlog Burn charter — the Trust Model burn

**Operator ruling (2026-08-16, verbatim intent):** "a full loop execution/burn on the TRUTH
SERUM backlog item and any other existing and completely frozen components — as soon as we hand
off and orient to a new session." Scope ruled via the priority-table review: **the trust-model
program + exactly three ride-alongs.**

## Intake (BBM triage-then-grill-once applies)

1. **The Trust Model program** — the converged GRILL-LEDGER compiled via DESIGN → PLAN → FROZEN
   (in flight). Its waves per TM-H1: engine+cursor first → skills migration → hook LAST.
2. **🔴 BL-X14** (vacuity prover vacuous on Linux) — **ordering PREREQUISITE**: the blocking
   mutation precondition activates per-platform only after X14 closes (R3-H3); also ends the
   CI-red-12-days state. Mini-grill at burn triage.
3. **BL-X15** (statusline empty-argv crash on Linux) — rides X14's run. Mini-grill at triage.
4. **🔴 BL-X12** (learn_feed emits OPEN as resolved) — unblocks all grill-close emits including
   this grill's own retroactive emit. Its recorded design fork (status vocabulary vs. refusing
   the DECISIONS route on ledger-marked files) is the triage question. Mini-grill at triage.

Explicitly OUT (operator table review, same sitting): X04/X08/X09/X10 (post-burn queue) · the
UX/presentation build wave (waits on round C1 + the operator's freeze sign-off) · everything
priority 6+ on the board.

## Binding rules for the executing session

- **BBM-12: the ENTIRE loop, wave-per-loop** — each wave is one loop iteration with its own
  default-FAIL final eval; failed eval ⇒ the wave re-loops (sibling child per R2-M2).
- Hybrid gating (BBM-1) · manual pinned worktrees outside the repo root (BBM-9) · builders
  briefed to push back (H7).
- D169: nothing dispatches without the FROZEN plan. The PLAN carries per-task `evidence:`
  declarations (TM-F1 — this burn's plan is the first under the rule; the field's closed
  grammar per RS-H1).
- **Model routing: Fable anchor** (operator-accepted recommendation) — judgment at Fable,
  builders auto-tier to Opus (D131).
- **The operator's sequencing mandate:** the burn starts ONLY after the full documented
  handoff + UX-15 agent orientation is committed. A dispatch before that is drift.
- Prose-era honesty: this burn BUILDS the seam — its own dispatches are pre-seam and are
  declared Honor-system in every status surface (Guardian; no bootstrap paradox pretense).

## Pipeline position at charter time

design-author dispatched (running) → conductor gate → plan-author (this charter binds its
intake) → conductor gate → FREEZE → handoff + orientation → NEW SESSION executes this charter.
