---
spec: ux-rework
kind: convergence-review record, round 1
date: 2026-08-16 (overnight, operator asleep)
verdict: CONVERGE-HOLD — 7 HIGH / 9 MED / 6 LOW on the compiled freeze-candidate DESIGN.md
resolution: design-author dispatched for revision 2 with per-finding instructions; conductor
  interim rulings below, EVERY one labeled in the DESIGN and re-presented at freeze
---

# UX DESIGN convergence round 1 — record

**The reviewer's seven HIGH findings, one line each:** (H1) the dot-collapse rule inverted UX-27/
UX-30 under a citation claiming to read them; (H2) UX-19's entire closeout option set (incl. the
inline grill dial + no-third-onramp constraint) was dropped in compilation; (H3) UX-6/UX-10 never
cited, and three LOCKED surfaces (branded execution output, phase-break block, doc grammar) had no
build step; (H4) the §5 "promote the generators' helpers" story is impossible — they are five
divergent HTML emitters — so the engine needs a specified multi-form contract with the generators
as golden fixtures; (H5) width-assert-RAISES vs live variable data is an unsatisfiable pair with
no truncation grammar; (H6) the four closeout stat boxes have NO surviving pixel reference (v4
lived in gitignored scratch); (H7) four of ten color-table roles contradicted the operator-approved
templates' actual locked CSS, incl. a factually false "no hex was ever locked" claim for dim.

## Conductor interim rulings (operator confirms or overrules at freeze)

1. **Glyph status set (H1):** keep ✓/⚠/✗ ● ○ ALL — outcomes vs live vs pending role split; no
   deletion of ● from the primary form.
2. **Engine shape (H4):** same semantics, fresh implementation, four output forms; generators
   demoted from import-sites to golden fixtures. Preserves UX-29's intent (one small engine, only
   new engine Python, widths asserted); changes only the mechanism the ledger never specified.
3. **Overflow (H5):** authored strings raise; variable data fields truncate with … at the measure.
4. **Color table (H7):** recompiled FROM the approved templates' CSS (dark-ground #d9a960 titles,
   #c2653a rust text, #F7F2E6 values, #a9b1c3 prose, #8fb3cc pale, #565f74/#44607a dim); SGR-2
   dim proposal withdrawn; the earth-tone dark-ground lightening generalized.
5. **Stat boxes (H6):** honestly marked "pixel reference LOST — reconstruction owed, operator
   re-approval required"; NOT fabricated as locked.
6. **Starfield:** seeded RNG per Determinism law 9 (randomness mints identity only).
7. All MED/LOW = mechanical ledger-fidelity restorations (UX-9 removal ruling, UX-19 option set,
   UX-21 platform honesty, UX-26 vitals split, UX-6 line-break rule, probe-4 conflation fix, UA
   label restored, §8 completeness, citation repairs).

## Round 2 (same night, fresh reviewer)

**Verdict: CONVERGE-HOLD — 3 HIGH / 3 MED / 3 LOW, all document-local.** Five of the seven R1
HIGHs verified genuinely closed (code-verified against the generators). Remaining: (R2-H1) the
recompiled color table matches the five generators but CONTRADICTS the frozen launch template on
three roles — title `#B5894B` (the literal "lock A") vs `#d9a960`, value `#c9d1d9` vs `#F7F2E6`,
command `#61afef` vs `#4d87ae` — resolved overnight by DISCLOSURE, not repainting; the per-role
reconciliation is an operator item. (R2-H2) rev 2 dropped UX-17's dot mapping and left ●
meaning two things — resolved by scoping: UX-17 verbatim on owned surfaces, an explicit labeled
NARROWING on glyph-only surfaces. (R2-H3) the wrapper's Python entrypoint shim is a carve-out of
UX-29's "only new Python" lock and was unlabeled — labeled + indexed. Author dispatched for
revision 3; the conductor verifies the three fixes line-by-line (no full round 3 — the
substantive calls sit on the operator list regardless).

## Owed to the operator at freeze (the morning list)

Confirm/overrule rulings 1–6 above · eyeball the recompiled color table **and reconcile the
launch-template palette divergence (which hex family wins per role: the frozen launch screen's
`#B5894B`/`#c9d1d9`/`#61afef` or the five later generators')** · confirm the UX-17 glyph-only
narrowing (dots keep color-carries-state on owned surfaces; outcomes move to ✓/⚠/✗ where color
is stripped) · confirm the wrapper Python-shim carve-out of UX-29 · approve the stat-box
reconstruction plan (UX-19's full per-box content list survives as the input, PLUS the UX-33
agent-type breakout ruled after rev 3 — the reconstruction must carry both) · the
[author-proposed] items the reviewer triaged as operator-class (preload seam config shape —
cheap to rule now, drift vector if deferred).
