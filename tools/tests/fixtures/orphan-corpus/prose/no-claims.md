# S2 fixture — prose containing no reuse-claim trigger phrase at all

This document describes a build with no architectural assertions about existing code. Nothing here
claims a surface exists. A zero-candidate scan of this file must be reported as zero-candidate —
never as "all citations resolve".
